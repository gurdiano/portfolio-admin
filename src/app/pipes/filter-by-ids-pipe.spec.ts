import { FilterByIdsPipe } from './filter-by-ids-pipe';

describe('FilterByIdsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByIdsPipe();
    expect(pipe).toBeTruthy();
  });
});
