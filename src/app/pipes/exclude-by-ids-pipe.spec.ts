import { ExcludeByIdsPipe } from './exclude-by-ids-pipe';

describe('ExcludeByIdsPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcludeByIdsPipe();
    expect(pipe).toBeTruthy();
  });
});
